- page_title(["#{@sub_category.name}", "#{@sub_category.category.name}"])
.col-sm-3
  = render "categories"

.col-sm-9
  .row
    .col-sm-12
      .pull-right
        - if admin?
          = link_to "Add Product", new_product_path, class: "btn btn-sm btn-primary"

  %br
  = render "slide_image"

  #products_container
    = render "products", products: @products, category: '', sub_category: ''